# 기본적인 산수값은 여기에서 처리한다.

# 서버 환경 설정
#server
server.port=8090

# 서버를 재시작
spring.devtools.livereload.enabled=true
spring.devtools.remote.restart.enabled=true

# mybatis
# 매퍼용 클래스가 위치한 경로
mybatis.type-aliases-package=com.example.demo.dto

# 매퍼가 어디 위치에 있는지 선언을 해준다.
# mapper xml 파일이 위치한 경로